парсер:
    функция по очистке char *champion_names[max_players]
    обрабоать число для флага -dump, не должно превыщать какое-то определенное число
каретка:
	изменить способ хранения и инициализацию
операции:
    Переделать live (смотри vbraznik), сделать aff - не так, как остальные операции
    get_args не читает приоперации, начинающейся на 4097
    там где записываешь или читаешь надо обрезать по MEM_SIZE иначе сега (sti, ldi и тд)
