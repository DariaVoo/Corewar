парсер:
    функция по очистке char *champion_names[max_players]
    обрабоать число для флага -dump,
    не должно превыщать какое-то определенное число
    --leak-check=full
    --leak-check=full
    parse_args(t_vm *vm, char *champion_names[MAX_PLAYERS], char **argv):
        исправить костыль с переменной limit (если игроков будет больше 4,
        и они все имеют номер (флаг -n) или наоборот, не имеют, то 5-й игрок
        записывается в чемпионы[MAX_PLAYERS], перезаписывает NULL и только потом кидает ошибку)